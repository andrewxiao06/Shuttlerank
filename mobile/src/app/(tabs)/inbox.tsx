import { Placeholder } from "../../../components/Placeholder";

// Inbox — approve/dispute pending matches. Real screen lands in Phase 3 (Claude).
export default function Inbox() {
  return <Placeholder title="Inbox" subtitle="Pending matches — Phase 3." />;
}
