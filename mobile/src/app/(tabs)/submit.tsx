import { Placeholder } from "../../../components/Placeholder";

// Submit match. Real screen lands in Phase 3 (Claude).
export default function Submit() {
  return <Placeholder title="Submit" subtitle="Match submission — Phase 3." />;
}
